[zabbix_vm]
zabbix-01 ansible_host=192.0.2.10 ansible_user=ubuntu

# Use a real inventory file, for example:
#   cp ansible/inventory/hosts.example.ini ansible/inventory/hosts.ini
# Then replace ansible_host and ansible_user.
